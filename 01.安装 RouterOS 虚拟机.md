# 01. 安装与配置 RouterOS 虚拟机 (CHR / OVA)

> 说明：本指南详细记录在 PVE 9 虚拟化平台上部署 **MikroTik RouterOS (CHR 官方云路由系统)** 作为家庭主路由的全流程。涵盖官方 RAW 磁盘镜像导入以及 **OVA 模板一键导入** 两种方式，并结合双 2.5G 网卡规划 WAN/LAN 接口、PPPoE 宽带拨号、NAT 防火墙以及将 DHCP 网关/DNS 指向 Keepalived 高可用 VIP (`10.10.11.10`) 的标准配置。

---

## 🌟 硬件与网络拓扑定位

- **虚拟机 ID**：`100`（或自定义，如 `RouterOS`）
- **CPU / 内存**：2 核 (Host) / 2048 MB RAM
- **网卡分配**：
  - `ether1` (绑定 PVE `vmbr1`)：**WAN 口**，物理直连光猫宽带上网口，进行 PPPoE 拨号或 DHCP 获取外网 IP。
  - `ether2` (绑定 PVE `vmbr0`)：**LAN 口**，连接局域网交换机/AP/各虚拟机业务网。
- **主路由 IP**：`10.10.11.11/24`
- **DHCP 下发策略**：
  - 地址池：`10.10.11.100 - 10.10.11.200`
  - **默认网关 (Gateway)**：`10.10.11.10`（指向 Debian 12 旁路由 Keepalived VIP）
  - **DNS 服务器 (DNS)**：`10.10.11.10`（指向 Debian 12 旁路由 Keepalived VIP，由 MosDNS 分流）

---

## 1. 方式一：导入官方 OVA 模板快速创建 (推荐)

MikroTik 官方提供了适用于虚拟化平台的 OVA 封装包：

1. **下载官方 OVA 镜像**：
   访问 MikroTik 官方下载页：[MikroTik Routers and Wireless - Software](https://mikrotik.com/download)，在 **Cloud Hosted Router (CHR)** 区域下载最新的 `RouterOS v7.x OVA Template`（例如 `chr-7.16.ova`）。
2. **上传 OVA 到 PVE 宿主机**：
   使用 SCP / SFTP 将文件上传至 PVE 的 `/tmp` 目录：
   ```bash
   scp chr-7.16.ova root@10.10.11.2:/tmp/
   ```
3. **在 PVE 终端使用 qm importovf 一键导入**：
   SSH 登录 PVE 9 宿主机：
   ```bash
   qm importovf 100 /tmp/chr-7.16.ova local-lvm --format qcow2
   ```
   > 提示：`100` 为分配给主路由的 VM ID，`local-lvm` 为 PVE 存储池名称。
4. **调整硬件网卡**：
   - 导入完成后进入 PVE WebUI -> 找到 VM 100；
   - 检查或添加两张 VirtIO 网卡：
     - **net0**：桥接选择 `vmbr1` (对应 WAN 口)；
     - **net1**：桥接选择 `vmbr0` (对应 LAN 口)。

---

## 2. 方式二：手动创建 VM 并写入 CHR RAW 镜像

若使用官方 RAW 镜像手动部署：

1. **创建空虚拟机**：
   - 常规：VM ID `100`，名称 `RouterOS`；
   - 操作系统：选择 **`不使用任何介质 (Do not use any media)`**，类别 `Linux (6.x - 2.6 Kernel)`；
   - 系统：默认 `i440fx` + `SeaBIOS`，SCSI 控制器选择 `VirtIO SCSI`；
   - 磁盘：先随意创建一个小磁盘（稍后分离删除）；
   - CPU：`2 核`，类别选择 `host`；
   - 内存：`2048 MB`；
   - 网络：添加两张 VirtIO 网卡，分别桥接 `vmbr1` (WAN) 与 `vmbr0` (LAN)；
   - 完成后在 `硬件` 中将临时创建的磁盘 **分离 (Detach)** 并 **删除 (Remove)**。
2. **下载并写入 CHR 镜像至虚拟机**：
   在 PVE 终端执行：
   ```bash
   cd /tmp
   # 下载最新的 RouterOS 7.x 镜像压缩包
   wget https://download.mikrotik.com/routeros/7.16/chr-7.16.img.zip
   unzip chr-7.16.img.zip
   
   # 将解压出的 img 导入为 VM 100 的虚拟磁盘
   qm importdisk 100 chr-7.16.img local-lvm
   ```
3. **挂载磁盘与设置引导**：
   - 返回 PVE WebUI -> VM 100 -> `硬件`；
   - 双击 **未使用的磁盘 0 (Unused Disk 0)** -> 总线选择 `SCSI (scsi0)` -> 点击 `添加`；
   - 切换到 `选项` -> 双击 `引导顺序 (Boot Order)` -> 将 `scsi0` 勾选并拖动至第一位。

---

## 3. 首次启动与基础登录

1. 点击 **`启动 (Start)`** 运行 VM 100，打开 `控制台 (Console)`；
2. 首次登录默认用户名：`admin`，默认密码：**为空 (直接按回车)**；
3. 系统会提示查看软件许可协议，按 `n` 拒绝/跳过；
4. 强制设置新密码：按提示输入强密码并确认；
5. 记录当前的 LAN 口 MAC 地址，或使用官方管理工具 **[Winbox](https://mikrotik.com/download)** 在同一局域网电脑通过 MAC 地址直接扫出并连接 RouterOS。

---

## 4. 网络接口与 IP 配置 (CLI 一键脚本)

在 Winbox 顶部打开 **`Terminal`**（命令行终端），将以下配置脚本按需调整后一次性粘贴执行：

```routeros
# ==========================================
# 1. 接口重命名与端口分组
# ==========================================
/interface set [find default-name=ether1] name=ether1-WAN comment="Physical WAN (vmbr1)"
/interface set [find default-name=ether2] name=ether2-LAN comment="Physical LAN (vmbr0)"

/interface list
add name=WAN
add name=LAN

/interface list member
add interface=ether1-WAN list=WAN
add interface=ether2-LAN list=LAN

# ==========================================
# 2. 配置主路由 LAN IP 地址
# ==========================================
/ip address
add address=10.10.11.11/24 interface=ether2-LAN network=10.10.11.0 comment="RouterOS Master LAN IP"
```

---

## 5. 上网配置 (PPPoE 拨号 / 光猫 DHCP)

### 选项 A：光猫桥接，RouterOS 负责 PPPoE 拨号 (推荐)
在 Terminal 中执行：
```routeros
/interface pppoe-client
add name=pppoe-out1 interface=ether1-WAN user="你的宽带账号" password="你的宽带密码"     add-default-route=yes use-peer-dns=no disabled=no comment="Internet PPPoE Dial"

/interface list member
add interface=pppoe-out1 list=WAN
```

### 选项 B：光猫拨号，RouterOS 通过 DHCP Client 自动获取 IP
```routeros
/ip dhcp-client
add interface=ether1-WAN add-default-route=yes use-peer-dns=no disabled=no comment="Internet WAN DHCP"
```

---

## 6. 配置 DHCPv4 服务 (网关/DNS 全权指向 Keepalived VIP)

在旁路由双机高可用拓扑中，主路由仅负责下发 IP 地址租约，而将流量网关与 DNS 引导至双旁路由虚拟 VIP (`10.10.11.10`)：

```routeros
# 1. 创建 DHCP 地址池 (10.10.11.100 - 10.10.11.200)
/ip pool
add name=dhcp_pool_lan ranges=10.10.11.100-10.10.11.200

# 2. 创建 DHCP 网络参数 (核心：网关与 DNS 设为 10.10.11.10)
/ip dhcp-server network
add address=10.10.11.0/24 gateway=10.10.11.10 dns-server=10.10.11.10     comment="DHCP for LAN Clients - Directed to Keepalived VIP"

# 3. 关联 DHCP 服务器到 LAN 口
/ip dhcp-server
add name=dhcp_lan interface=ether2-LAN address-pool=dhcp_pool_lan     lease-time=1d disabled=no
```

---

## 7. 基础安全防火墙与 NAT 转发 (IPv4)

```routeros
# ==========================================
# 1. 出口 NAT 伪装规则 (Masquerade)
# ==========================================
/ip firewall nat
add chain=srcnat out-interface-list=WAN action=masquerade comment="Default WAN NAT Masquerade"

# ==========================================
# 2. 基础过滤规则 (允许已建立连接，阻断非法外网入站)
# ==========================================
/ip firewall filter
# 允许来自已建立和关联状态的数据包
add chain=input action=accept connection-state=established,related comment="Accept established/related input"
add chain=input action=accept in-interface-list=LAN comment="Accept all LAN management input"
add chain=input action=accept protocol=icmp comment="Accept ICMP ping"
add chain=input action=drop in-interface-list=WAN comment="Drop invalid WAN input"

add chain=forward action=accept connection-state=established,related comment="Accept established/related forward"
add chain=forward action=accept in-interface-list=LAN out-interface-list=WAN comment="Accept LAN to WAN traffic"
add chain=forward action=drop connection-state=invalid comment="Drop invalid forward traffic"
```

---

## 8. 验证与网络联通性测试

1. 在 Winbox 中点击 **`Tools` -> `Ping`**；
2. 输入公共 IP `223.5.5.5` 点击 `Start`，确认能够正常收到 Ping 回包；
3. 将局域网电脑/手机接入局域网（通过交换机或 AP）：
   - 检查终端获取到的 IP 是否在 `10.10.11.100 - 10.10.11.200` 范围内；
   - 检查终端的默认网关与 DNS 是否为 **`10.10.11.10`**；
4. 测试终端设备访问国内网站（直连）与海外服务（走旁路由 eBPF daed + Sing-box 加速），确认整套软路由与双旁路由集群协同正常！

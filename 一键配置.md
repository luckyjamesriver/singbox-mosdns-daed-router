## 🚀 Debian 12 旁路由一键全自动部署指南 (One-Click Setup)

> 专为追求极致效率与稳定性的用户打造。在 Proxmox VE (PVE) 中新建好 Debian 12 虚拟机并完成基础网络配置后，只需**执行一条命令**，即可在 2 分钟内全自动安装并调优 **MosDNS v5 + Sing-box 1.14+ + MetaCubeXD + daed (eBPF)** 全套核心架构。

---

### 📋 1. 前置准备条件 (PVE 创建 Debian 12 阶段)

在运行一键脚本前，请确保您的 Debian 12 虚拟机满足以下 3 项基础条件：

1. **已配置好静态 IP 与网关**：
   - 旁路由本机 IP：如 `10.10.11.8/24`（或 `10.10.11.7/24`）；
   - 主路由网关：如 `10.10.11.1`（RouterOS 主路由 IP）；
   - 基础 DNS：`223.5.5.5` 或 `119.29.29.29`。
2. **网络连通性正常**：
   - 在 Debian 终端执行 `ping -c 3 10.10.11.1` 能通；
   - 能正常访问互联网（可以访问 GitHub 与 Debian 官方源）。
3. **已开启 SSH**：
   - 可以通过 SSH 客户端（Termius / FinalShell / 终端）以 `root` 权限登录。

---

### ⚡ 2. 一键全自动部署命令

以 `root` 用户登录 Debian 12 终端，直接复制并粘贴执行以下命令：

```bash
bash <(curl -fsSL https://raw.githubusercontent.com/luckyjamesriver/A-side-router-on-Debian-12/main/setup.sh)
```

> **提示**：脚本运行时会自动检测您的网卡名称（如 `ens18`）、静态 IP 和网关，并询问确认。直接敲击 **回车 (Enter)** 即可全自动开始！

---

### 🛠️ 3. 脚本在后台全自动完成的动作

| 组件 | 自动化执行内容 |
| :--- | :--- |
| **🛠️ Linux 内核调优** | 1. 自动关闭并禁用冲突的 `systemd-resolved`，**彻底释放 53 端口**；<br>2. 开启 `net.ipv4.ip_forward = 1` 路由转发与 **Linux BBR** 拥塞控制；<br>3. 解除 eBPF 内存锁限制（`memlock unlimited`）。 |
| **🌐 MosDNS v5.3+** | 1. 下载安装最新版 MosDNS 并配置 Systemd 开机自启守护；<br>2. 自动拉取国内白名单规则集（`geosite_cn`、`geoip_cn`）与**阿里/淘宝全系强制直连规则**，彻底解决“该区域无法销售”等电商 IP 误判；<br>3. 监听 `0.0.0.0:53`（TCP/UDP），作为局域网高性能 DNS 解析器。 |
| **⚡ Sing-box 1.14+** | 1. 导入 SagerNet 官方最新 **deb822** 标准源进行安装；<br>2. 配置轻量 Socks5 入站（监听 `0.0.0.0:7891`）；<br>3. 部署 **MetaCubeXD** 现代 Web 仪表板（监听 `:9090`），并配置每日凌晨 03:00 自动更新。 |
| **🚀 daed (eBPF)** | 1. 自动拉取并安装最新版 `daed` 软件包；<br>2. 启动内核级 eBPF 透明代理守护服务，监听 `0.0.0.0:2023` Web 管理面板。 |

---

### 📱 4. 安装完成后的快速收尾 (只需 2 步)

脚本执行完毕后，终端将输出一份精美的**完工看板**，您只需要进行以下两项收尾配置：

#### 第一步：浏览器打开 `daed` 管理面板
1. 在局域网电脑浏览器中打开：
   ```text
   http://10.10.11.8:2023
   ```
2. 初次打开设置管理员账号与密码；
3. 点击右上角 ✏️ 铅笔图标进入设置：
   - **LAN 接口 / WAN 接口**：选择您的网卡名称（如 `ens18`）；
   - **DNS 上游**：填写 `tcp+udp://127.0.0.1:53`（指向本地 MosDNS）；
4. 添加节点 / 订阅链接，并在路由规则中绑定节点；
5. 点击右上角 **运行** 按钮，启动 eBPF 透明分流！

---

#### 第二步：在 RouterOS 主路由配置 DHCP 下发
登录您的 RouterOS 网页后台或 Winbox：
- 进入 **IP** -> **DHCP Server** -> **Networks**；
- 将 **Gateway (Option 3)** 填写为：`10.10.11.8`（或您的 Keepalived VIP `10.10.11.10`）；
- 将 **DNS Server (Option 6)** 填写为：`10.10.11.8`（或 VIP `10.10.11.10`）。

🎉 **至此，全屋所有有线与无线设备均已全自动享受极速分流、超低延迟与防污染解析！**

---

### 📖 5. 想深入了解各模块技术细节？
如果您希望了解每个组件的手动配置过程与底层设计原理，欢迎查阅本仓库的分步详解章节：
- [00. 安装 PVE9](./00.安装%20PVE9.md)
- [01. 安装 RouterOS 虚拟机](./01.安装%20RouterOS%20虚拟机.md)
- [02. 安装 Debian12 虚拟机](./02.安装%20Debian12%20虚拟机.md)
- [03. 安装 sing-box](./03.安装%20sing-box.md)
- [04. 安装 mosdns](./04.安装%20mosdns.md)
- [05. 安装 daed](./05.安装%20daed.md)
- [06. 安装 keepalived](./06.安装%20keepalived.md)

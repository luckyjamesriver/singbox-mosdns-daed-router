## 安装与配置 Keepalived 高可用旁路由

> 为了防止旁路由故障，克隆了PVE主路由虚拟机做为”keepalived“的”备份“，分别安装并配置了keepalived。

---

### 🌟 架构与高可用设计

1. **虚拟路由冗余 (VRRP 协议)**：
   - 两台虚拟机（主机 A 与备机 B）通过 VRRP 组播进行心跳维持。
   - 共同持有虚拟路由 IP (VIP): `10.10.11.10`。
   - 局域网各终端设备（PC / 手机 / TV / 游戏机）以及 DHCP 选项中，直接将**网关**与 **DNS** 指向 VIP `10.10.11.10`。
2. **双 BACKUP + 非抢占模式 (`nopreempt`)**：
   - 双方状态均设为 `BACKUP`，配合 `nopreempt`。
   - 当原 Master 节点因维护重启或故障修复恢复后，**不会强行抢回 VIP**，杜绝因节点频繁上下线造成的局域网网络震荡与 TCP 连接重置。
3. **业务级健康检查脚本 (`check_dns.sh`)**：
   - 仅检测虚拟机存活是不够的；如果核心服务（如 mosdns / daed）崩溃或卡死，VIP 应当主动漂移至备用机。
   - 通过 `vrrp_script` 周期性检测本地 DNS 解析能力，异常时动态扣减优先级触发平滑漂移。

---

### 1. 安装 Keepalived

在**主机（A 节点）**与**备机（B 节点）**上分别执行以下命令安装 Keepalived 及健康检查工具：

```bash
sudo apt-get update
sudo apt-get install keepalived dnsutils -y
```

---

### 2. 编写业务健康检查脚本 (`/usr/local/bin/check_dns.sh`)

在**两台节点**上均创建健康检查脚本。该脚本检测本机 MosDNS 是否能够正常响应解析请求（探测国内白名单域名与局域网解析）：

```bash
sudo tee /usr/local/bin/check_dns.sh << 'EOF'
#!/bin/bash
## 检测本机 mosdns (53 端口) 是否正常提供 DNS 服务
## -T 2: 超时时间 2 秒
## +tries=1: 重试 1 次
if dig @127.0.0.1 -p 53 baidu.com +time=2 +tries=1 > /dev/null 2>&1; then
    exit 0
else
    exit 1
fi
EOF

## 赋予执行权限
sudo chmod +x /usr/local/bin/check_dns.sh
```

---

### 3. 主机（Master / 节点 A）配置

- **主机物理 IP**：`10.10.11.7`（示例）
- **虚拟漂移 IP (VIP)**：`10.10.11.10`
- **网卡名称**：`ens18`（请根据实际 `ip a` 或 `ip link` 确认）
- **初始优先级**：`100`

编辑主机配置文件 `/etc/keepalived/keepalived.conf`：

```bash
sudo nano /etc/keepalived/keepalived.conf
```

写入以下配置：

```text
global_defs {
    max_auto_priority
    script_user root
    enable_script_security
}

vrrp_script chk_dns_logic {
    script "/usr/local/bin/check_dns.sh"
    interval 3      # 每 3 秒检查一次
    fall 3          # 连续 3 次失败才降级（约 9 秒）
    rise 2          # 连续 2 次成功才恢复
    weight -25      # 失败则扣除 25 分 (100 - 25 = 75 < 90，触发降级漂移)
}

vrrp_instance VI_1 {
    state BACKUP          # 配合 nopreempt，必须设为 BACKUP
    interface ens18       # 请确认你的网卡名
    virtual_router_id 51  # VRRP 路由组 ID，同一局域网内主备机必须严格一致
    priority 100          # 初始优先级 100
    advert_int 1          # VRRP 报文广播间隔 (1 秒)
    nopreempt             # ⭐ 开启非抢占模式

    authentication {
        auth_type PASS
        auth_pass hbj.007 # 局域网组播认证密码，主备机保持一致
    }

    virtual_ipaddress {
        10.10.11.10/24    # 虚拟路由 VIP
    }

    track_script {
        chk_dns_logic
    }
}
```

---

### 4. 从机（备份机 / 节点 B）配置

在 PVE 中克隆生成的备份虚拟机上配置：
- **备机物理 IP**：`10.10.11.8`（示例，需保持与 A 节点同网段且不冲突）
- **虚拟漂移 IP (VIP)**：`10.10.11.10`
- **网卡名称**：`ens18`
- **初始优先级**：`90`（低于主机的 100）

编辑备机配置文件 `/etc/keepalived/keepalived.conf`：

```bash
sudo nano /etc/keepalived/keepalived.conf
```

写入以下配置：

```text
global_defs {
    max_auto_priority
    script_user root
    enable_script_security
}

vrrp_script chk_dns_logic {
    script "/usr/local/bin/check_dns.sh"
    interval 3
    fall 3          # 连续 3 次失败才降级（约 9 秒）
    rise 2          # 连续 2 次成功才恢复
    weight -25
}

vrrp_instance VI_1 {
    state BACKUP          # 备机始终为 BACKUP
    interface ens18       
    virtual_router_id 51
    priority 90           # 优先级设为 90，低于 A 节点
    advert_int 1
    nopreempt             # 备机也可以加 nopreempt 保持逻辑一致

    authentication {
        auth_type PASS
        auth_pass hbj.007
    }

    virtual_ipaddress {
        10.10.11.10/24
    }

    track_script {
        chk_dns_logic
    }
}
```

---

### 5. 启动与开机自启

分别在两台节点上启动 Keepalived 并加入开机自启：

```bash
sudo systemctl daemon-reload
sudo systemctl enable keepalived --now
sudo systemctl status keepalived
```

---

### 6. 验证 VIP 与故障转移测试

#### 6.1 查看 VIP 绑定状态
在**主机 A**上执行：
```bash
ip addr show ens18
```
若主机正常运行，网卡上会看到次级虚拟 IP：
```text
inet 10.10.11.10/24 scope global secondary ens18
```
而**备机 B**上此时不持有该 VIP。

#### 6.2 模拟故障转移与平滑切换测试
1. **测试服务故障转移**：
   在主机 A 上模拟 DNS 服务停机：
   ```bash
   sudo systemctl stop mosdns
   ```
   - 主机 A 上的 `check_dns.sh` 连续检测失败；
   - 主机优先级自动扣减为 `75`（低于备机的 `90`）；
   - VIP `10.10.11.10` 自动瞬时漂移绑定至备机 B。
   - 局域网各终端网络无感知，继续正常上网。
2. **测试恢复与非抢占**：
   在主机 A 上恢复服务：
   ```bash
   sudo systemctl start mosdns
   ```
   - 主机优先级回升至 `100`；
   - 得益于 `nopreempt`（非抢占模式），VIP 会继续驻留在当前稳定工作的备机 B 上，不会产生无谓的二次网络震荡。
3. **查看 Keepalived 实时运行日志**：
   ```bash
   journalctl -u keepalived -f -o cat
   ```

---

### 7. 客户端与主路由配置调整

高可用搭建完成后：
- **主路由 (如 ROS / OpenWrt)** 的 DHCP 服务中，将广播下发给局域网客户端的 **网关 (Gateway)** 与 **DNS** 统一修改为 VIP 地址：
  ```text
  网关: 10.10.11.10
  DNS:  10.10.11.10
  ```
- 从此即使其中任何一台旁路由虚拟机发生故障、宕机、内核崩溃或 PVE 重启维护，另一台备份机都会在数秒内无缝接管网络流量，保障全家网络永不断网！

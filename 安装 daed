# 安装与配置 daed (适配 v1.27.0+ 最新版本)

> **daed** 是基于 Linux 内核级 **eBPF** 技术开发的高性能透明代理路由守护程序（dae 的 Web GUI 版本）。它直接在操作系统内核的网络数据链路层完成透明分流与流量转发，免去了繁杂脆弱的 iptables 规则链，具备极高吞吐与极低 CPU 负载。

---

## 1. 下载与安装最新版 daed

当前 daed 最新正式发布版本为 **v1.27.0**。针对 Debian 12 (x86_64 / arm64) 执行以下命令安装：

```bash
# 1. 确认系统架构并下载对应 deb 安装包
ARCH=$(uname -m)
case "${ARCH}" in
  x86_64|amd64) DAED_ARCH="x86_64" ;;
  aarch64|arm64) DAED_ARCH="arm64" ;;
  *) echo "暂不支持的架构: ${ARCH}"; exit 1 ;;
esac

# 2. 下载 v1.27.0 官方 deb 包
wget -O /tmp/daed.deb "https://github.com/daeuniverse/daed/releases/download/v1.27.0/installer-daed-linux-${DAED_ARCH}.deb"

# 3. 安装并清理临时包
sudo dpkg -i /tmp/daed.deb
rm -f /tmp/daed.deb
```

---

## 2. 启动与管理服务

```bash
# 启动并设置开机自启
sudo systemctl enable daed --now

# 检查运行状态
sudo systemctl status daed

# 实时查看 daed 日志
journalctl -u daed -f -o cat
```

---

## 3. WebUI 界面交互式配置指南

服务启动后，在局域网电脑浏览器中打开：
```text
http://10.10.11.7:2023
```
点击界面右上角铅笔图标即可进入配置中心。

### 3.1 全局设置 (Global)
在 WebUI 中根据以下参数进行配置：

- **软件选项**：
  - **日志等级**：`warn`
  - **透明代理端口**：`12345`
  - **透明代理端口保护**：`Enabled` (开启)
  - **为 dae 设置套接字标记 (Socket mark)**：`0`
- **接口及内核选项**：
  - **LAN 接口**：`ens18`（按实际网卡名称选择）
  - **WAN 接口**：`ens18`（按实际网卡名称选择）
  - **自动配置内核参数**：`Enabled` (开启)
  - **MPTCP**：`Disabled` (关闭)
  - **启用本地 TCP 快速重定向**：`Disabled` (关闭)
- **连接选项**：
  - **拨号模式 (Dial Mode)**：`domain++`
  - **允许不安全 (Allow Insecure)**：`Disabled` (关闭)
  - **禁用网络等待**：`Enabled` (开启)
- **节点连通性检测**：
  - **检测间隔**：`30s`
  - **检测公差**：`0s`
  - **嗅探超时**：`100ms`
  - **TCP 检测 HTTP 方式**：`HEAD`
  - **TCP 检测链接**：`http://cp.cloudflare.com`，`1.1.1.1`
  - **UDP 检测 DNS**：`dns.google:53`，`8.8.8.8`
- **TLS**：
  - **TLS 实现**：`utls`
  - **uTLS 模仿**：`chrome_auto`
- **DNS**：
  - **备用解析器**：`8.8.8.8:53`
- **带宽限制 (Bandwidth)**：
  - **最大上行带宽**：`200 mbps`
  - **最大下行带宽**：`1 gbps`

---

### 3.2 DNS 设置 (DNS)
点击 DNS 标签页，将 DNS 查询全面委托给本地的 MosDNS (`:53`)：

```text
ipversion_prefer: 4
upstream {
  mosdns: 'tcp+udp://127.0.0.1:53' # 本地 mosdns 服务
}
routing {
  request {
    fallback: mosdns
  }
}
```

---

### 3.3 路由分流规则 (Route)
点击 Route 标签页，填入以下实际运行的生产分流规则：

```text
# 1. 拦截无效流量
ipversion(6) -> block
l4proto(udp) && dport(443) -> block

# 2. 核心 AI 平台加速
domain(suffix: openrouter.ai) -> proxy
domain(suffix: anthropic.com) -> proxy
domain(suffix: claude.ai) -> proxy
domain(suffix: openai.com) -> proxy
domain(suffix: ai.google.dev) -> proxy

# 3. 出口进程绝对直连 (防止环路)
pname(sing-box) -> must_direct
pname(tailscaled) -> direct
domain(suffix: roonlabs.net) -> direct

# 4. DNS 防环路 (仅针对 53 端口)
pname(mosdns) && l4proto(udp) && dport(53) -> must_direct

# 5. 内网与私有服务
dip(10.10.11.0/24) -> direct
dip(224.0.0.0/3) -> direct
dip(geoip:private) -> direct

# 6. 国内流量白名单
domain(geosite:cn) -> direct
domain(geosite:xiaohongshu) -> direct
domain(suffix: deepseek.com) -> direct
domain(suffix: qianwen.com) -> direct
domain(suffix: taobao.com) -> direct
domain(suffix: tmall.com) -> direct
dip(geoip:cn) -> direct

# 7. 特殊分流规则
domain(suffix: split.io) -> direct

# 8. Mosdns 策略接入点
pname(mosdns) -> must_rules

# 优先匹配 OpenAI & ChatGPT 关联域名
domain(
  geosite:openai,
  chatgpt.com,
  openai.com,
  oaistatic.com,
  oaiusercontent.com,
  sentry.io,
  intercom.io
) -> proxy

# 9. 默认兜底
fallback: proxy
```

---

### 3.4 节点配置 (Node)
1. 点击左侧菜单「**节点**」-> 点击右上角「**+**」新建节点。
2. 填写节点信息以连接 Sing-box：
   - **链接 (Link)**: `socks5://127.0.0.1:7891`
   - **名称 (Tag)**: `ToWorld`
3. 保存节点。

---

### 3.5 群组与启动 (Group)
1. 进入「**群组**」界面；
2. 将刚才创建的 `ToWorld` 节点拖入代理群组；
3. 点击界面右上角的「**运行 / 重载**」（闪电或运行按钮）。
4. 状态变为绿色/蓝色即表示 eBPF 透明代理已正式生效接管！

---

## 4. 路由规则库每日自动更新

创建 `/usr/share/daed/dae_rule_update.sh`：

```bash
cat > /usr/share/daed/dae_rule_update.sh <<\x27EOF\x27
#!/bin/bash
set -e

download() {
  local url="$1"
  local dest="$2"
  local tmp="$dest.tmp"
  if wget -q -O "$tmp" "$url"; then
    mv -f "$tmp" "$dest"
    echo "$dest 更新成功"
  else
    echo "$url 下载失败" >&2
  fi
}

download "https://github.com/v2rayA/dist-v2ray-rules-dat/raw/master/geoip.dat" "/usr/share/daed/geoip.dat"
download "https://github.com/v2rayA/dist-v2ray-rules-dat/raw/master/geosite.dat" "/usr/share/daed/geosite.dat"

# 重载 daed 规则
systemctl restart daed
EOF

chmod +x /usr/share/daed/dae_rule_update.sh
```

添加每日定时任务 (`crontab -e`)：

```text
0 4 * * * /usr/share/daed/dae_rule_update.sh >/dev/null 2>&1
```

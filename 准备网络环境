# 旁路由准备网络环境 (Debian 12)

> 说明：本指南基于 ESXi 虚拟机安装的 Debian 12（单网卡 ens33，最小化安装，仅含基础工具与 SSH Server）。
> 家用网络环境示例：
> - 旁路由 Debian 静态 IP：`10.10.11.7`
> - 主路由 IP (网关与 DHCP 服务器)：`10.10.11.11`
> - 子网掩码：`255.255.255.0` (`/24`)

---

## 1. 配置 SSH 允许 Root 登录

安装完成后，登录 root 用户进行基础远程连接配置：

```bash
nano /etc/ssh/sshd_config
```

找到 `PermitRootLogin` 取消注释并改为 `yes`，或者在文件末尾直接追加：

```text
PermitRootLogin yes
```

保存后重启 SSH 服务：

```bash
systemctl restart ssh
systemctl status ssh
```

确认输出状态为 `active (running)` 即表示正常。

---

## 2. 确认网卡名称与配置静态 IP

### 2.1 查看当前网卡名称

```bash
ip a
```

通常输出中除 `lo` 外的网卡即为主网卡（例如 `ens33` 或 `eth0`，请记录您的网卡名称）。

### 2.2 配置静态 IP 与关键 DNS

编辑网络接口文件：

```bash
nano /etc/network/interfaces
```

将网卡配置修改为静态 IP。**特别注意：必须配置 `dns-nameservers`**，否则 Debian 12 重启后由于未配置静态 DNS，会导致 `/etc/resolv.conf` 为空从而无法解析任何域名（包括 GitHub）：

```text
source /etc/network/interfaces.d/*

# 本地环回接口
auto lo
iface lo inet loopback

# 主网卡配置 (以 ens33 为例)
auto ens33
allow-hotplug ens33
iface ens33 inet static
    address 10.10.11.7/24
    gateway 10.10.11.11
    # 核心：必须指定静态 DNS，首选主路由，备用公共 DNS
    dns-nameservers 10.10.11.11 223.5.5.5 119.29.29.29
```

保存文件后，重启网络或重启系统：

```bash
systemctl restart networking || reboot
```

---

## 3. 安装必备工具包

通过 SSH 客户端（如 Termius / FinalShell / 终端）登录 `ssh root@10.10.11.7`：

```bash
apt update && apt upgrade -y
# 安装常用管理工具、网络诊断工具及防火墙规则持久化工具
apt install wget curl unzip sudo vim net-tools iputils-ping iptables iptables-persistent traceroute dnsutils -y
```

> 安装 `iptables-persistent` 时若弹出交互界面询问是否保存当前规则，选择 **Yes** 即可。

---

## 4. 彻底禁用 IPv6（可选，推荐纯 IPv4 环境）

若家庭网络未配置 IPv6 或为了避免代理组件因 IPv6 产生漏网流量，建议彻底禁用 IPv6：

```bash
cat > /etc/sysctl.d/99-disable-ipv6.conf <<EOF
net.ipv6.conf.all.disable_ipv6 = 1
net.ipv6.conf.default.disable_ipv6 = 1
net.ipv6.conf.lo.disable_ipv6 = 1
EOF

# 使配置立即生效
sysctl -p /etc/sysctl.d/99-disable-ipv6.conf
```

执行 `ip a` 检查，若所有网卡均无 `inet6` 地址，则说明 IPv6 已彻底关闭。

---

## 5. 内核转发与旁路由核心优化（🔴 关键）

旁路由必须开启内核 IPv4 转发；更重要的是，**必须关闭 ICMP 重定向 (Send Redirects)**！
> **为什么必须关闭 ICMP 重定向？**  
> 在旁路由架构中，客户端流量发给旁路由（10.10.11.7），旁路由处理完国内直连流量后，会将其转发回同网段的主路由（10.10.11.11）。  
> 此时 Linux 内核默认会发出 ICMP Redirect 数据包通知客户端“直接找主路由”，导致客户端绕过旁路由甚至断网，引发严重的非对称路由故障。

创建旁路由专用系统内核配置文件：

```bash
cat > /etc/sysctl.d/99-side-router.conf <<EOF
# 1. 开启 IPv4 数据包转发
net.ipv4.ip_forward = 1
net.ipv4.conf.all.forwarding = 1
net.ipv4.conf.default.forwarding = 1

# 2. 彻底禁用 ICMP 重定向 (旁路由生命线)
net.ipv4.conf.all.send_redirects = 0
net.ipv4.conf.default.send_redirects = 0
net.ipv4.conf.ens33.send_redirects = 0

# 3. 启用 BBR 拥塞控制算法 (提升代理传输吞吐与稳定性)
net.core.default_qdisc = fq
net.ipv4.tcp_congestion_control = bbr
EOF

# 使内核配置立即生效
sysctl -p /etc/sysctl.d/99-side-router.conf
```

### 放行并持久化 iptables FORWARD 规则

旁路由转发流量必须保证 iptables FORWARD 链默认允许通过，且必须持久化保存（防止重启失效）：

```bash
# 设置转发链为接受
iptables -P FORWARD ACCEPT

# 持久化规则至系统，防止重启丢失
netfilter-persistent save
```

---

## 6. 常用别名与环境便利设置

编辑 `~/.bashrc`：

```bash
vim ~/.bashrc
```

在末尾或对应区域添加常用别名：

```bash
export LS_OPTIONS="--color=auto"
eval "$(dircolors)"
alias ls="ls \$LS_OPTIONS"
alias ll="ls \$LS_OPTIONS -l"
alias l="ls \$LS_OPTIONS -lA"

alias rm="rm -i"
alias cp="cp -i"
alias mv="mv -i"
```

保存后执行使其生效：

```bash
source ~/.bashrc
```

至此，Debian 12 旁路由底层网络环境准备完毕，网络具备高吞吐转发与绝对稳定的无重定向转发能力。
